# ===========================================
# 1103-OSS Configuration File
# S3-Compatible Object Storage System
# ===========================================
# 
# 这是唯一的配置文件，同时用于：
# 1. Docker Compose 容器配置
# 2. Go 应用程序运行时配置
#
# 修改后需要重启服务：
# cd deployments && docker compose --profile dev restart
# ===========================================

# -----------------------------------------
# Database Configuration
# -----------------------------------------
# PostgreSQL connection settings
DB_PASSWORD=oss_password

# Optional database settings (使用默认值)
# DB_HOST=postgres
# DB_PORT=5432
# DB_NAME=oss
# DB_USER=oss

# -----------------------------------------
# Admin User Configuration
# -----------------------------------------
# Initial admin user credentials
# 这些凭证会在首次启动时创建管理员账户
ROOT_USER=admin
ROOT_PASSWORD=admin123

# -----------------------------------------
# Web Frontend Configuration
# -----------------------------------------
# API URL for Web Frontend
# 设置前端访问的后端API地址
# 本地开发: 留空（自动使用 http://localhost:9000）
# 生产环境: http://your-server-ip:9000 或 http://your-domain.com:9000
API_URL=

# -----------------------------------------
# Initial S3 Access Credentials
# -----------------------------------------
# S3-compatible access key and secret key
# 如果留空，会在首次启动时自动生成
# 生成后会自动写入此文件，请妥善保管
INIT_ACCESS_KEY=
INIT_ACCESS_SECRET=

# -----------------------------------------
# Docker Compose Profiles
# -----------------------------------------
# Development mode (使用热重载，端口: 9000, 9001, 3000)
# COMPOSE_PROFILES=dev

# Production mode (生产构建，端口: 9000, 9002)
# COMPOSE_PROFILES=production

# -----------------------------------------
# Advanced Configuration (Optional)
# -----------------------------------------
# Server settings
# OSS_SERVER_HOST=0.0.0.0
# OSS_SERVER_PORT=9000
# OSS_SERVER_REGION=us-east-1

# Storage settings
# OSS_STORAGE_TYPE=local
# OSS_STORAGE_LOCAL_BASE_PATH=/data/oss

# Security settings
# OSS_AUTH_TOKEN_EXPIRE_HOURS=24

# Environment
# ENV=production

# -----------------------------------------
# 生产环境部署提示
# -----------------------------------------
# 1. 修改 ROOT_PASSWORD 为强密码
# 2. 妥善保管自动生成的 INIT_ACCESS_KEY 和 INIT_ACCESS_SECRET
# 3. 使用强 DB_PASSWORD
# 4. 设置正确的 API_URL
