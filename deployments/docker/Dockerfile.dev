# Development Dockerfile with hot reload
FROM golang:1.21-alpine

WORKDIR /app

# Install air for hot reload (compatible version for Go 1.21)
RUN go install github.com/cosmtrek/air@v1.49.0

# Copy go mod files
COPY go.mod go.sum ./
RUN go mod download

# Copy source code
COPY . .

# Expose port
EXPOSE 9000 9001

# Run with air for hot reload
CMD ["air", "-c", ".air.toml"]
