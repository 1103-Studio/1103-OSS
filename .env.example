# ===========================================
# 1103-OSS Configuration File
# S3-Compatible Object Storage System
# ===========================================

# -----------------------------------------
# Database Configuration
# -----------------------------------------
# PostgreSQL connection settings
# These values will be used by both the application and docker-compose

# Database password (required)
# Default: oss_password
DB_PASSWORD=oss_password

# Database host (optional, default: postgres in docker-compose)
# DB_HOST=postgres

# Database port (optional, default: 5432)
# DB_PORT=5432

# Database name (optional, default: oss)
# DB_NAME=oss

# Database user (optional, default: oss)
# DB_USER=oss

# -----------------------------------------
# Admin User Configuration
# -----------------------------------------
# Initial admin user credentials
# These will be created on first startup

# Admin username (default: admin)
# Used for web console login and initial S3 credentials
ROOT_USER=admin

# Admin password (default: admin123)
# IMPORTANT: Change this in production!
ROOT_PASSWORD=admin123

# -----------------------------------------
# Initial S3 Access Credentials
# -----------------------------------------
# S3-compatible access key and secret key
# If left empty, they will be auto-generated on first startup
# and saved back to this .env file
#
# These credentials are used for S3 API authentication
# Leave empty for auto-generation (recommended for first run)

# S3 Access Key (will be auto-generated if empty)
INIT_ACCESS_KEY=

# S3 Secret Key (will be auto-generated if empty)
INIT_ACCESS_SECRET=

# -----------------------------------------
# Server Configuration (Optional)
# -----------------------------------------
# API server settings
# Uncomment to override defaults

# Server host (default: 0.0.0.0)
# OSS_SERVER_HOST=0.0.0.0

# Server port (default: 9000)
# OSS_SERVER_PORT=9000

# AWS region (default: us-east-1)
# OSS_SERVER_REGION=us-east-1

# -----------------------------------------
# Storage Configuration (Optional)
# -----------------------------------------
# Object storage backend settings

# Storage type: local | distributed (default: local)
# OSS_STORAGE_TYPE=local

# Local storage base path (default: /data/oss)
# This path is inside the container
# OSS_STORAGE_LOCAL_BASE_PATH=/data/oss

# -----------------------------------------
# Security Configuration (Optional)
# -----------------------------------------
# Authentication and security settings

# Token expiration time in hours (default: 24)
# OSS_AUTH_TOKEN_EXPIRE_HOURS=24

# -----------------------------------------
# Environment
# -----------------------------------------
# Application environment
# Options: development | production (default: production)
# ENV=production

# -----------------------------------------
# Notes
# -----------------------------------------
# 1. All environment variables with OSS_ prefix will override
#    corresponding values in configs/config.yaml
#
# 2. For production deployment:
#    - Change ROOT_PASSWORD to a strong password
#    - Keep INIT_ACCESS_KEY and INIT_ACCESS_SECRET secure
#    - Use strong DB_PASSWORD
#
# 3. Database credentials (DB_*) are used by docker-compose
#    to initialize PostgreSQL container
#
# 4. Admin credentials (ROOT_*) are used to create the initial
#    admin user in the application database
#
# 5. S3 credentials (INIT_ACCESS_*) are used for S3 API access
#    and can be managed via the web console after first login
